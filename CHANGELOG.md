# Changelog

<!-- 撰寫規則摘要（完整版見 AGENTS.md「CHANGELOG 撰寫規則」）：
  - bullet 寫給玩家，會整段照貼 Workshop 更新說明：症狀先行、遊戲內名詞、
    禁檔名/函式名/行號/引擎術語；影響版本誠實寫清楚
  - 「> 技術要點：」（選用）只放管理員/modder 需要的行為事實；單次變更的完整
    技術原因寫在 commit message 本文（綁 diff、可 git log 搜尋、永不公開）
  - 紅線：不寫攻擊配方（安全修正只寫「強化了驗證」）、不寫玩家名/座標/Steam ID、
    不寫主機名/路徑/IP -->

所有重要的變更都會記錄在此檔案中。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號遵循 `{PZ版本}-{主版本}.{次版本}.{修訂}` 格式。

## [Unreleased]

### 新增

- 自動駕駛語音提示：啟動、關閉、前方受阻煞停、倒車脫困、無法通過交還操控、抵達目的地時各念一句。只有自己聽得到；預設跟隨遊戲語言——繁中／簡中說中文、日文說日語、其他語言說英語；ESC 模組選項與 MiniMap 設定的「語音語言」可改指定語音包（Chinese／English／Japanese，名稱與本體語言清單同款）。可在 HUD、ESC 模組選項或 MiniMap 設定關閉
- HUD 新增「語音」開關與音量拉桿（0–100、滾輪微調；放開拉桿會念一句試聽），以及第三種主題「家族卡片」：頂部標題條放狀態、現速與四個控制，下方兩列放巡航、主按鈕與檔位
- 前方被車陣或路障堵死、煞停等待時，HUD 狀態字旁會出現「改道」鈕：按下向導航系統要一條繞開堵點的替代路線，找到就立刻換線繼續走（綠字提示＋語音），找不到會用紅字說明。ESC 模組選項與 MiniMap 設定另有「自動改道」開關（預設關）：開啟後停等累計約 10 秒、倒車重掃也沒用時，自動要一次替代路線，同一次停等只試一次
- 第四種 HUD 主題「側掛雙翼」：不再蓋在儀表板上方，而是兩片與儀表板同高同材質的側翼分別貼在儀表板左右緣，儀表板上方的路面完全露出來。左翼放狀態、現速、巡航與主按鈕，右翼放檔位、策略、電量油量與四個設定控制；**左右可以各自收放**，各自記在角色身上——想省空間就只留左翼常駐，右翼收成一顆小徽章。畫面太窄時會自動先摺右翼、再摺左翼
- HUD 按鈕的說明提示改用重點上色：判定條件、開啟／關閉策略、仍然有效的安全限制、數值各用不同顏色，一眼找得到重點
- 語音提示重新配音，語氣更鮮明：啟動、抵達更興奮，受阻更急，交還操控更抱歉；三種語言同步
- HUD 的減速藥丸旁多一顆「自動改道」藥丸（繞行箭頭圖示，綠＝開、灰＝關；點一下切換），與 ESC 模組選項、MiniMap 設定裡的「堵死時自動改道」是同一個開關，四種主題都有
- HUD 的控制鈕改成圖示：樣式（調色盤）、收合／展開（箭頭）、語音（喇叭，亮綠＝開、灰＝關）、改道（繞行箭頭），殭屍／屍體減速藥丸也改成圖示（綠＝正在減速、灰＝關）；說明滑鼠移過去看提示。四種主題都因此變窄，金屬主題右側方塊從兩個拉桿寬縮成一顆方鈕加一條拉桿

### 變更

- 「樣式」與「隱藏」按鈕從原版儀表板右上角的小鈕移到 HUD 本體上，三種主題各有自己的擺法：金屬擬物＝右側 2×2 方塊、簡約玻璃＝第一列一排三顆＋第二列右端音量拉桿、家族卡片＝標題條。「樣式」鈕在三種主題間循環；收合後的徽章上就有「展開」鈕

### 修正

- 修正較長路線（約 7 公里以上、彎道較多）全程只能開 18 公里／小時的問題：彎道總數超過內部圓角處理容量時，整條路線的平滑圓角與路面帶驗證會一起被放棄，安全閘門於是把直路也壓在近場警戒速度。現在容量只影響最後面的彎道（改為保留折點爬行），前段照常平滑過彎；容量本身也提高一倍。以玩家回報的 7.4 公里路線離線重建：全程可達路線上限、最慢彎道約 27 公里／小時，實機仍待回報確認。此問題自 0.2.0 首發即存在
- 診斷紀錄補上路線建立結果（圓角數、退化折點數、路面帶驗證是否有效與退化原因）與原始路線點列、路寬、路面，離線即可重跑同一條路線的規劃，不必再靠猜
- 診斷紀錄檔頭改帶本 MOD 的正式版號（與 Workshop 相同的 `modversion`，舊版是一個久未更新的內部印記）、遊戲版本與當時啟用的全部 MOD 及其版本；回報問題時只要附紀錄檔，就能確認跑的是哪一版、開了哪台 MOD 車
- 貼身擠縫（縫隙只比車身寬一點、只能以個位數時速通過的那種繞行）只用在單一障礙：若擠過這個縫之後路線馬上又被擋住（車陣、連續廢車），就直接判定「無法通過」交給改道／交還操控，不再花三十秒爬進去、撞上、倒車三次才放棄。出口後淨空的單一障礙維持原本「物理上過得去就過」的行為
- 修正在路線外側（例如停在路肩或對向車道）啟動自動駕駛時，車子原地不動、導航線每秒重畫幾次的問題：回到路線的過程中，掃描範圍的中心在「目標車道」與「目前位置與目標的中點」之間來回切換，每一輪都把上一輪算好的回線判成範圍外而重來，每次重來都把油門歸零。現在整段回線期間掃描範圍固定錨在起點與目標的中點。此問題自 0.1.0 加入回線即存在，離線測試一直沒有涵蓋「線外啟動」這個情境
- 修正「改道」幾乎永遠回報找不到替代路線的問題：舊版把避讓範圍的圓心放在堵點上，車子本身就在圓內，任何從目前這條路出發的替代路線一開頭就穿過避讓範圍而被拒收，只剩起點在別條路上的路線又因起點太遠被拒。現在圓心改放在堵點再往前推一個半徑處：車在圓外、整段車陣仍被覆蓋，從本路倒回去繞別條路的替代線可以被接受
- 修正被拒收的替代路線（起點太遠、繞太遠）在下一次取路時仍被當成一般換線收下的問題：導航系統一算出替代線就會覆寫快取，即使自動駕駛拒收了；現在拒收過的那條線不會再被沿用，等導航系統重算後的新路線才換線
- 修正靠近要繞行的廢棄車時速度掉到 4 公里／小時、而且縫隙還在二十幾公尺外就開始爬行的問題：接近繞行縫的減速距離是用「完全鬆油滑行」的減速度反推的（每秒 0.6 公尺），時速 43 公里時算出約 118 公尺，等於一看到要繞的車就先爬行；而且是「進入範圍就整段套用縫隙限速」的開關式判定，車一慢下來範圍就縮小、限速解除又加速、再套用——逐格來回震盪，平均下來就是全程爬行。現在改成用煞車減速度反推的連續速度曲線：縫隙還遠就照常行駛，越接近才平順收斂到縫隙該有的速度（同一條 20 公尺的接近段，從 4 提高到約 36 公里／小時）。此問題自 0.1.0 引入繞行即存在
- 修正導航因偏航重算路線時，若新路線的起點被吸到幾十公尺外的另一條路上，自動駕駛會照著開進樹林的問題（0.2.0 加入的「起點太遠就拒絕啟動」只檢查啟動與更換目的地兩個時機，行駛中重算沒有檢查）。現在每次換線都檢查，超過 20 公尺一律紅字交還操控
- 修正車陣裡兩台車之間明明有 2.8 公尺的縫，自動駕駛卻判定「無法通過」的問題：舊版把每台停著的車當成它壓到的每一格地板（一台 1.8 公尺寬的車被膨脹成 3 格＋安全圈，等於 4.4 公尺寬），並排車之間的縫因此被吃到剩 0.2 公尺。現在停著的車改用它真正的車身輪廓（車頭車尾四角＋周圍一圈安全距離）計算，並排車之間、車陣裡的縫都以真實寬度判斷；車身資料讀不到的車仍照舊用地板格判定
- 修正 ESC 模組選項勾了「自動改道」也永遠不會自動要替代路線的問題：舊版在「停等累計 5 秒先倒車重掃」之後才輪得到改道判定，但倒車回來又被堵住那一刻，倒車判定會先一步搶走這一輪，改道條件永遠湊不齊，最後只會倒車三次就紅字交還。現在「倒車重掃過一次、開回原地又被同一處堵住」就直接要替代路線（不再等第二次倒車），停等累計 10 秒的門檻也保留；自動改道問一次沒路，才回到倒車／紅字的階梯。此問題自 0.2.0 加入自動改道即存在
- 修正在路線起點離車很遠（例如車停在樹林、田野裡，導航把路線起點吸到幾十公尺外的平行道路上）時仍允許啟動，車子直接往路上開結果卡死在樹林裡的問題：現在起點距離超過 20 公尺就拒絕啟動並提示「請先開到那條路上，或把目的地點在你所在的道路上」，導航中換目標也套用同一道檢查（交還操控）。此檢查需要 MiniMap 主 MOD 0.24.0 以上（導航 API v5）；搭配舊版主 MOD 時不啟用，以免沿路開一段、停車後再啟動被誤判成「起點太遠」
- 修正大型車（皮卡、卡車）在住宅區路口右轉時切進路口內側、卡在圍籬或電線桿上的問題：路網上不少「直角」路口其實是 90 多度（例如 90.9°），舊版把超過 90 度整數的路口都當成急折、不做平滑圓角而保留尖折點慢速通過；轉彎半徑大的車根本轉不出尖角，只能一路切進內側。現在略大於 90 度的路口照常做圓角，只有真正的急折（120 度以上、迴轉）才保留折點
- 修正靠右行駛偏置在彎道內側把車擠出路面的問題：窄路右轉時，圓角本身已經用掉路面內側的空間，再疊上靠右偏置就會車身出路肩、擦到路口內側的圍籬。現在偏置會依路面剩餘空間自動收——窄路右轉先讓到路中間再轉（與人開車相同），出彎再回到右側；直路上的偏置也不再超出路面（5 公尺寬的路上會自動收窄）。此問題自 0.1.0 加入靠右偏置即存在
- 修正多人伺服器開著速限（伺服器設定 SpeedLimit，預設 70）時，自動駕駛的直路速度明顯低於手動油門到底的問題（巡航設 70 實際只跑 51）：伺服器速限會把車速「放大」後才拿去和定速比較，自動駕駛的定速值沒有做同一套換算，於是提早斷油。現在寫入定速前先做相同換算；單機或速限 120 以上的伺服器不受影響。此問題自 0.1.0 首發即存在
- 同一句語音提示短時間內不再連續重播（受阻煞停時七秒念了六次）：上一句還沒念完不插播，念完也要隔 8 秒才會再念同一句；換成別的提示仍會立刻蓋過舊句

> 技術要點：圓角容量為路線點數 64→128、取樣輸出 512→1024，超出仍只退化尾段彎道；診斷紀錄格式仍為 v1、純新增欄位（header 多 `game`／`mods`，`build` 改為 mod.info 的 modversion；route ready 事件多四欄、route cutover 事件多三欄字串），既有分析工具不受影響。改道走主 MOD 導航 API v3 的 `requestDetour`（避讓圈半徑 40 公尺、以最近擋線點為圓心），本 MOD 端再驗收：仍穿過避讓圈、起點超過 20 公尺、長度超過剩餘路程的 1.5 倍＋200 公尺一律拒收；接受後避讓圈會黏著這趟導航，主 MOD 之後自行重算若又穿回堵點會再要一次替代線。需要主 MOD 支援 API v3（更舊的主 MOD 只會顯示找不到替代路線）。停著的車輛改由 `getScript():getExtents()`／`getCenterOfMassOffset()`＋`getWorldPos` 取 OBB 四角（與引擎碰撞多邊形同式），周長每 0.3 公尺一點、半徑 0.15 公尺（引擎自己的 polyPlusRadius 安全圈）；長車放大步距、單台上限 64 點、點雲緩衝不足 64 時整台退回地板格，任何 getter 失敗也退回地板格。繞行接近段改用 `MDADDynamics.approachCapKmh`（到縫口距離、縫隙限速、反應 0.5 秒、safeBrake×0.7 反推的單調曲線；出口速為 0 時與可視距離限速同一二次式），二值 slowZone 退役。診斷紀錄新增 `dodgeApproachCap`（接近段限速，可與 `dodgeSpeedCap` 分辨「還很遠就壓速」與「縫隙本身的限速」）；貼縫死路判定＝`crawlDeadEnd`：squeeze／physical 檔候選在掃掠通過後，若出口 d+1 之後仍有擋線點即拒收（console `sweep deadend[...]`）；cruise 檔與出口後淨空的貼縫不受影響。改道避讓圓心＝堵點沿「車→堵點」方向再推 `DETOUR_AVOID_R`（40 公尺），sticky 避讓與主 MOD 收到的圓同一定義；被拒收的替代線以 table identity 記在 session（`rejectedRoute`），cutover 跳過，主 MOD 重算的新 identity 照常換線。回線期間掃描帶錨點統一為 `returnScanBias`（起點↔目標中點；跨距塞不進走廊才退回跟隨現 lane）。

## [42.20.4-0.2.0] - 2026-09-02

### 新增

- 新增可選的自動駕駛診斷紀錄，預設關閉；開啟後只在本機固定紀錄槽寫入含絕對世界座標與原始 epoch 毫秒時間戳（不做時區轉換）的紀錄檔。新版 MiniMap「自動駕駛」分類可複製最新檔或資料夾的絕對路徑；MiniMap API v1 與 ESC 選項仍可改開關及保留天數，但沒有複製按鈕
- 新增段落索引，可依每段紀錄的開始／結束 timestamp、檔案大小與結束原因快速找到正確檔案；仍維持每次自動駕駛一個檔，不把同日多段行駛混在一起
- 複製最新紀錄或資料夾路徑成功時，改用右上角通知顯示完成回饋；舊版 UI 框架或通知失敗時仍會顯示頭上提示
- 診斷紀錄補上碰撞前後的規劃狀態，以及最近障礙的尺寸與位置，能更快分辨是感知、規劃或車輛控制出了問題
- 開啟診斷紀錄時會一併記下當下的引擎／煞車／輪胎與路況物理狀態，方便判斷低速蠕動是偏離路面、在煞車、輪胎打滑還是機械限制；關閉診斷時自駕行為不變
- 自動駕駛現在會依實際車型的車寬、車長、軸距、轉向角、重量、引擎、煞車與輪胎狀態
  調整前視距離、轉向力臂、碰撞掃掠與加減速能力；小車、廂型車與長軸皮卡不再共用
  同一組固定車身假設。
- 搭配 MiniMap 導航 API v4 時，自動駕駛會讀取每段道路的寬度與鋪裝／碎石／土路分類，
  合法碎石路與土路不會因遊戲判定為越野就反覆拉回鋪裝；舊版 MiniMap 仍以保守的
  未知路面模式正常跟線。
- 車輛偏離行駛線時會先建立並完整掃掠平滑回線軌跡；資料未載入、目前車道受阻、
  車身過長或缺少可靠煞車時會停住並顯示原因，不再沿未驗證斜線或錯誤車道繼續前進。
- 導航 API v4 的一般彎道會在可行道路寬度內改成平滑圓角（底層圓弧相切，行駛折線以最長 1 公尺、切線誤差不超過 2° 取樣）；塞不進路面帶的折點保留並爬行。前方彎道只參與 coast 預降，只有車身已進入圓角段且實速突破當段硬曲率包絡才完整煞停
- 全速行駛現在必須同時通過感知就緒與新鮮、完整煞停距離、走廊與車身掃掠、跟線狀態、回線完成、連續對正、進度健康、圓角與路面帶證明；110 公尺不足以安全停下時會依實際可視距離降速
- 繞行速度改依側移長度、曲率、剩餘淨距與可視距離動態計算；寬裕靜態障礙可更流暢通過，車輛與窄縫仍維持低速

### 移除

- 暫時移除「道路完全堵死時自動更換路線」：實測它常拿到品質更差的替代路線，而且
  每次重算都會把已經試過的繞行記憶清掉，反而更容易卡死。現在遇到完全堵死的路段
  只會就地尋找繞行縫、停等，逾時後交還操控並提示；請手動重設目的地或換一條路。
  自動更換路線會在路線品質問題解決後再回歸

### 修正

- 修正製作自動駕駛模組時，材料區內沒裝電池的導航儀會被整批吞掉的問題（開發版實測：
  39 個導航儀一次製作後全部消失、只做出 1 個模組）。現在每次製作固定只消耗 1 個
  導航儀，而且沒電的導航儀也可以直接當材料，不必先裝電池
- 修正多人伺服器上玩家開啟某些屍體背包時，伺服器紀錄被同一則錯誤大量洗版的問題。
  此問題自開發版加入生成開關起即存在；道具生成與生成過濾本身不受影響

- 修正開發版自動駕駛完全失效：車輛選單的自動駕駛開關沒有作用、駕駛 HUD 不出現，
  且遊戲訊息紀錄被同一行錯誤持續洗版而蓋掉真正的原因。現在自動駕駛核心可正常載入；
  萬一未來核心又載入失敗，HUD 會只留一行說明後安靜停用，不再洗版
- 修正道路看起來淨空時仍長時間被限制在 15 km/h：路肩障礙、玩家已選擇忽略的屍體，
  或已由彎道速度包絡處理的折點，不再把整條前方路線誤判成不安全；實際行駛線完成
  車身掃掠後會恢復檔位、車輛與沙盒允許的速度；原有動態物與軟障礙限速仍保留，
  並在偵測當幀立即生效，不會從高速慢慢滑降
- 修正長車在看似淨空的直路上逐漸偏向路肩、撞到路緣物後誤進脫困，以及安全證明因
  煞停距離外的未載入區域或道路帶邊界而長時間鎖在低速；現在會提早消除反向積分、
  依橫向偏離補足回線轉向，並只要求目前煞停視界內的道路帶、載入範圍與車身掃掠證明
- 放寬過度保守的繞行與重車起步：停放車不再被當成行進車套低速帽，側移會利用前後
  可用距離拉長成較平順的軌跡，整格障礙的圓形近似會補償 0.1m 量化肥邊；重量較大的
  車在低速且仍低於最終安全目標時，會把有界前推合併進同一次轉向衝量，安全限速、
  煞車、倒車與調頭狀態仍可立即停用前推
- 修正自動駕駛進行中無法複製最新紀錄路徑；現在不必先停止自動駕駛
- 修正長車或車身已有偏角時，前方規劃線看似淨空卻仍由目前車身擦撞障礙；現在每輪感知完成都會以車身實際中心與尺寸做獨立碰撞守門
- 修正卡住後可能反覆倒退撞回同一處、帶著倒車速度立刻重新向前，以及後方未知或被車牆擋住仍嘗試倒車；現在會先檢查後方、倒車期間持續重查，成功後先停穩，並保留同一目的地的失敗路線記憶
- 轉彎與跟線整體更順、更果斷：確定可過的路段全油門；急彎才減速、離彎道很遠時不再
  提早鬆油；沿路速度改由剩餘餘裕連續縮放，不再動不動就爬行；轉向改以前輪端施力，
  轉頭方向與車身側移同向，彎中蛇行與貼線誤差明顯下降；非道路與重車的推力輔助加大
- 修正三種「導航線不沿路」的狀況：路口與轉角處的三角形倒鉤（自動駕駛自己的圓角輸出
  順序錯，會先跑到取樣點再退回圓弧起點，經過時被壓到 12 km/h）現已消除；彎道切弦與
  部分街道轉角斜穿院子則是遊戲內建街道資料本身的折線問題，轉角一例已由 MiniMap
  本體的道路修補層更正
- 修正調頭後停住不動、重新啟動也卡住、要玩家推一下車才走：調頭出來還沒擺正時回線
  系統會搶先接手，車頭外一小段圍籬讓回線一直驗不過而原地等到超時。現在頭沒擺正不
  進回線、回線等待靜止超過 2 秒會自動交還一般跟線並冷卻，不再卡死
- 修正在窄路口轉彎時明顯頓挫（幾乎停下再起步）：路口折點附近的橫向偏差是切彎的
  幾何必然，不再觸發回線；進入回線的等待期改為低速爬行而非急煞
- 繞行、回線與調頭三套系統改為單一持有權仲裁（調頭 > 繞行 > 回線），不再互相清掉
  對方剛建立的軌跡而原地打結；解析式速度公式一律只壓速不否決通行，是否可過只由
  世界掃掠決定
- 診斷紀錄槽滿時改為覆寫最舊的紀錄，不再拒寫；紀錄檔頭補上開發版本戳

> 技術要點：client-only、預設關；固定 64×2MiB、每 session 一檔；保留天數 1／3／7／14／30（預設 7）；`session-index.txt` 以 raw epoch ms 固定最多 64 列。JSONL 保留既有欄位並 additive tg/rg/eid/ps/ua/ban/ud/rear/rf/rms/ac/pc/fb/fhx/fhy/af，以及 fullGate/gateReason/cmdV/cmdA/jerkBypass/curveValid/curveKappa/curveHardActive/laneCurveEnvelope/envelopeBuildLat/envelopeBuildCoast/visibilityCap/filletN/filletFallbackN/dodgeDesignSpeed/dodgeBaseCap/dodgeCapPending/dodge caps。Vehicle profile 每 session 冷路徑建一次 32 段 block range-min tree；v4 fillet 的底層圓弧相切，行駛折線採 1m／2° 取樣，world-line proof 不改原 route identity。build `m68-20260831a`。

## [42.20.4-0.1.2] - 2026-08-31

### 修正

- 修正 0.1.1 新增的多人登入補配方沒有對主角色生效；已超過電工 6／8 級的既有角色，重新進入遊戲後現在會正常補上 GPS 導航儀／自動駕駛模組配方，不必再靠升級或恢復技能觸發

## [42.20.4-0.1.1] - 2026-08-30

### 新增

- 新增電子導航維修手冊；閱讀一本即可學會 GPS 導航儀與自動駕駛模組兩份配方，實際製作仍分別需要電工 3／6 級
- 找到 GPS 導航儀可在電工 3 級研究 GPS 配方；找到自駕模組可在電工 3 級研究 GPS、電工 6 級再研究自駕配方，研究不會消耗成品；電工 6／8 級自動學會的原有路徑仍保留

### 修正

- 修正多人遊戲中，MOD 安裝前已超過自動學習門檻的角色完成製作讀條後，材料沒有消耗、成品也沒有產出的問題；登入時伺服器現在會補齊符合門檻的兩份配方

## [42.20.4-0.1.0] - 2026-08-30

### 新增

- 新增 GPS 導航儀與自動駕駛模組，可從電子／軍用物資取得或使用電子技能合成
- 導航儀與自駕模組可安裝到車輛；卸下導航儀時保留原有電量
- 新增導航道具需求、自駕模組需求、GPS／自駕各自的合成與世界搜刮生成開關、四組獨立電量／燃油倍率，以及自駕速度上限等沙盒設定
- 新增車輛 radial 自駕開關；自駕會沿導航路線跟線、依彎道與車頭偏差調整速度並在抵達時停車
- 玩家操作方向、油門或煞車時，自駕會立即交還控制
- 自駕預設靠右行駛（偏移量可由沙盒調整或關閉），與對向車自然錯開；繞行障礙時暫時借道再回到右車道
- 偵測到障礙開始繞行、堵住煞停、倒車脫困、恢復控制時都會在頭上顯示提示，減速永遠有理由可見
- 自駕啟動期間會顯示半透明連續軌跡線：藍色為正常路徑，黃色為障礙繞行路徑；可在 MOD Options 或新版 MiniMap 的「自動駕駛」分類開關，並選擇 1／3／7 像素的細／標準／粗單線。粗線只增加畫面像素填充、不增加伺服器負擔；舊版 MiniMap 仍可使用 MOD Options。停止自駕後線立即消失，紅色障礙圈仍只供除錯模式使用
- 車輛卡住無法前進持續數秒時，自駕會先嘗試倒車脫困；退不出去才關閉並顯示提示
- 自駕會掃描路線前方的障礙物：路面有空隙就自動小幅繞過，繞不過則煞停等待並提示，障礙清除後自動恢復行駛
- 前方殭屍密集、有地面屍體、行進中的車輛或區域尚未載入時，自駕會自動減速；殭屍／屍體只套用專屬限速，不當成障礙物繞行或停車，HUD 滑鼠提示會列出判定範圍、分級限速與伺服器鎖定狀態
- GPS 與自駕的耗電、額外油耗各自可設 0–500%，同時使用時相加；GPS 導航中的車輛預設增加原生實際油耗 5%、自駕增加 25%，引擎發電機仍照原版為電瓶充電
- 繞行軌跡改為世界座標連續折線（轉角平滑），多候選枚舉＋世界掃掠複驗；速度依實測餘裕 10-28 分段，接近段不提前減速
- 堵死時自動改道：導航以堵點為避讓圈重算路線，小地圖同步顯示新路線
- 物理卡住（輪轉但零進度）先換繞行縫再倒車脫困；MP 同步抖動下的「假動」車輛以位置比對收編為障礙
- 沙盒速度上限超過 85 時啟用高速檔：掃描帶拉長到 110 公尺、上限開放到 120 km/h
- 終點與進彎改按真實煞停距離收油，不再提前減速
- 新增駕駛 HUD：與原版車輛儀表可見邊緣重疊融合，金屬主題沿用原版灰黑色與斜角樣式；顯示自駕狀態、車速、巡航上限、檔位、減速政策與電油量，可直接啟停、切檔；原版儀表內建收放鈕與 M/S 主題切換鈕，ESC 選單開啟時自動隱藏；HUD 與原版儀表同屬一般 UI 層，沙盒選項與管理視窗會正常蓋在其上方
- 駕駛 HUD 提供 30／50／70 km/h 與「瘋狂（車輛極速）」四檔；首發版的瘋狂檔先沿用穩定的跟線、過彎與障礙安全機制，目前主要差異是巡航上限，後續版本將再調整為更激進的駕駛行為
- 自駕寫入原版定速器的目標速度改為整數，原版車輛儀表不再顯示十多位小數
- 支援單人與多人；多人拆裝與資源消耗皆由伺服器依實際操作者重新驗證，client 不能指定其他玩家、裝置、耗用量或時間

### 修正

- 修正都市道路明明有足夠空間時，繞過停放車輛仍長時間只跑約 14 km/h；現在會在同一側多留安全餘裕，寬縫進入時可提升至 24 km/h、貼行後最高 28 km/h，真正窄縫仍維持低速或煞停
- 修正相鄰停車場使用相同柏油地板時，路面置中會把停車場與街道誤認成一條超寬道路，以及切換路線後沿用舊方向校正，導致自駕貼向路緣石碑或鐵欄；過寬或被掃描邊界截斷的鋪面現在會退回導航街道中心線，換線時也會重設校正
- 修正十字路口切換路段或橫向回線時，車輛雖持續移動、沿線進度卻暫時不變，因而被誤判卡住並反覆倒車；推撞兜底現在改以車體實際世界位移判定

> 技術要點：舊開發版的單一耗電倍率已完整拆成 GPS／自駕兩組電量倍率；既有測試存檔若只留舊鍵，四組新倍率會各自採 100% 預設。
